torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Apparatus coordinate prediction
scikit-learn>=1.3.0

# Hardware backends (install ONE matching your hardware)
# rppal             # Raspberry Pi GPIO / I2C
# pwm-pca9685       # PCA9685 servo driver (recommended for 6+ servos)
# pyserial          # Arduino serial protocol

# Transport (optional — NCP frames work over any byte stream)
# redis>=4.0.0

# Additional environments
# gymnasium>=0.29.0
